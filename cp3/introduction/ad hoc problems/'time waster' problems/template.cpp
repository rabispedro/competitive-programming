#include<bits/stdc++.h>
#define fastio ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL)
#define debug(x) cout << #x << "'= " << x << "'\n"

using namespace std;

int main() {
	fastio;
	return 0;
}