#include<bits/stdc++.h>
#define fastio ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL)

using namespace std;

string str;

int main() {
	fastio;
	
	while(getline(cin, str)) cout<<str<<"\n";
	return 0;
}
